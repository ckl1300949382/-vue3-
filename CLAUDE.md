# 项目说明(Claude 必读)

## 跨会话续接枢纽

**本项目是一个 AI Agent 驱动的教学项目**:用户在把"用户管理系统"的后端从 Node Express 重写为 Spring Boot + MySQL,同时学习"用 agent 做工程"的方法论(主面前端岗,agent 能力是差异化卖点)。

**每次会话开始,必须先读 `docs/学习档案.md`**,里面包含:
- 教学目标与定位(agent 第一主线,后端最低必要理解)
- 8 阶段教学规划与当前进度(当前停在哪个阶段)
- 学习画像与学习问答记录(用户水平、习惯、已解决的问题)
- 关键契约事实(前端 9 接口契约,以前端代码 `src/api/user.js` 为准)

## 教学铁律

1. **阻塞式教学**:每阶段先讲概念课(大白话+比喻+类比前端),用户确认理解后才能进入实现
2. **绝不允许"一句话生成整个后端"**:每段代码必须经过用户理解和验收,每写完一个文件要解释关键代码
3. **复盘双轨**:每阶段结束同时沉淀「后端概念面试点」和「agent 故事」(任务→拆解→agent 实现→发现问题→修正)
4. 用户 = 工程师(拆需求、批准任务、审查代码、验证),Claude = agent
5. 每阶段结束更新学习档案的「当前进度」和「学习问答记录」

## 项目现状

- 前端:Vue3 + Vite + Element Plus + Pinia(已完成,勿随意改动)
- 旧后端:`server/`(Node Express + users.json,保留作对照,阶段 7 验收通过后删除)
- 新后端:待建 `backend/`(Spring Boot 3.5.16 + MySQL,端口 3001)
- 部署:GitHub Pages(推 master 触发),线上仅静态展示
