# 复制本文件为 .env 后按需修改（.env 已被 gitignore，不会提交到仓库）
# cp .env.example .env

# 服务端口
PORT=3001

# JWT 签名密钥：生成随机串，如 `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
JWT_SECRET=your-random-secret-here
